# FTIR Annotator - UI/UX 設計方案

## 🎨 設計理念

```
專業工具 (不是玩具)
├── 清晰的信息層級
├── 高效的工作流程
├── 專家友好的互動
└── 可靠的反饋機制
```

---

## 📐 整體佈局

### 推薦：3 欄布局 + 頂部欄

```
┌─────────────────────────────────────────────────────┐
│  FTIR Spectrum Annotator    [Settings] [Export] [?]  │
├──────────────┬──────────────────────────┬────────────┤
│              │                          │            │
│   控制面板    │     光譜圖表              │   結果面板  │
│              │     (Plotly)            │            │
│  1. Upload   │                          │  Peaks:    │
│  2. Detect   │   ═════════════════      │  Position  │
│  3. Match    │       ▲ O-H              │  Intensity │
│  4. Review   │       │                  │  FWHM      │
│              │       │ ▲▲               │  Top Match │
│              │   ────┼──────────────    │  Confidence│
│              │       │ ▲ C-H            │            │
│              │       │                  │  ⚠️ Ambig  │
│              │   ════════════════       │  [Details] │
│              │                          │            │
├──────────────┴──────────────────────────┴────────────┤
│  Status: Idle | Peaks: 47 | Annotated: 47 | OK: ✓   │
└─────────────────────────────────────────────────────┘
```

### 寬度比例

```
控制面板: 20% (250px)
光譜圖:  50% (600px)
結果面板: 30% (360px)

> 1920px 時最佳
```

---

## 🎯 三欄詳細設計

### 左欄：控制面板 (250px)

```
┌─ CONTROLS ────────────────┐
│                            │
│  📁 UPLOAD                 │
│  ┌──────────────────────┐ │
│  │ 拖拽 CSV 文件        │ │
│  │ 或點擊上傳           │ │
│  └──────────────────────┘ │
│  📊 example_ethanol.csv   │
│  ✓ 3601 points            │
│                            │
│  ─────────────────────── │
│                            │
│  🔬 PEAK DETECTION         │
│  Smooth: [========●━━] 11 │
│  Poly: [========●━━] 3    │
│  Baseline: [Linear ▼]     │
│  Min Height: 0.005        │
│  Prominence: 5%           │
│  [🔍 Detect Peaks]        │
│  Status: Ready            │
│                            │
│  ─────────────────────── │
│                            │
│  🏷️  RULE MATCHING          │
│  Ambiguity: [====●━━] 5%  │
│  [🔗 Match Rules]         │
│  Status: Ready            │
│                            │
│  ─────────────────────── │
│                            │
│  ✏️  REVIEW                 │
│  ☑ Show Ambiguous         │
│  ☑ Show Secondaries       │
│  Filter: [All ▼]          │
│  [👁 Review Panel]        │
│                            │
└────────────────────────────┘
```

### 中欄：光譜圖 (600px)

```
┌─ SPECTRUM ─────────────────────────────────────────┐
│ 4000         3500         3000    2500   2000  1500 │ ← 波數 cm⁻¹
│  │             │           │       │      │     │   │
│100%           ┐            ┐            ┐           │
│               │  O-H       │            │   C=O     │
│  %T           │  broad     │  C-H        │   sharp  │
│               │            │ stretch    │           │
│50%            │            └──────      │           │
│               │                         │           │
│               │                         └───────    │
│               │                                      │
│ 0%            └──────────────────────────────────── │
│                                                      │
│ [🔍] Click peaks to see details | [📊 Export SVG] │
│                                                      │
│ Peak info: Position 3401 | FWHM: 298 | SNR: 15.2  │
└────────────────────────────────────────────────────┘

互動功能:
✓ Hover: 顯示波數、%T、信息
✓ Click: 選擇峰，右側展開詳情
✓ Zoom: 滾輪放大/縮小
✓ Pan: 拖拽移動
```

### 右欄：結果面板 (360px)

#### 上半部分：選中峰的詳情

```
┌─ PEAK DETAILS ──────────────────┐
│                                  │
│ Peak #15 @ 1715.23 cm⁻¹         │
│ Intensity: 0.8234 A             │
│ FWHM: 45.23 cm⁻¹               │
│ SNR: 18.45                       │
│                                  │
│ ─────────────────────────────── │
│ PRIMARY MATCH                    │
│ ┌──────────────────────────────┐│
│ │ 🥇 C=O Stretching (Ketone)  ││
│ │    Confidence: 87.0% ▓▓▓▓   ││
│ │    Shape: ✓ Sharp            ││
│ │    Intensity: ✓ Medium       ││
│ │    Secondary: 2 peaks found  ││
│ │    [ℹ️ Show Scoring]         ││
│ └──────────────────────────────┘│
│                                  │
│ ALL CANDIDATES (Top 5)           │
│ ┌──────────────────────────────┐│
│ │ 2. Ester          84.0% ▓▓▓  ││
│ │ 3. Carboxylic Acid 71.0% ▓▓ ││
│ │ 4. Amide          52.0% ▓   ││
│ │ 5. Unknown        45.0% ▓   ││
│ └──────────────────────────────┘│
│                                  │
│ ⚠️  AMBIGUOUS (gap: 3%)          │
│ [👁 Compare Candidates]         │
│                                  │
└──────────────────────────────────┘
```

#### 下半部分：峰表 (可切換)

```
┌─ PEAKS TABLE ───────────────────┐
│ Filter: [All ▼] Sort: [Pos ▲]  │
│                                  │
│ #  Position   FWHM  Top Match    │
│────────────────────────────────  │
│ 1  3401.2    298    O-H Alc ✓   │
│ 2  2960.5    45     C-H Alk ✓   │
│ 3  1715.2    45     C=O Ket ⚠️  │
│ 4  1465.8    30     C-H Bend ✓  │
│ 5  1085.3    50     C-O Str ✓   │
│ ...                               │
│                                  │
│ [📥 Select Multiple] [⬇️ Export] │
└──────────────────────────────────┘
```

---

## 🎨 色彩方案

### 專業深色主題 (推薦)

```css
/* 背景 */
--bg-primary:     #0f1419   /* 極深灰 */
--bg-secondary:   #1a1f2e   /* 深灰 */
--bg-tertiary:    #252d3d   /* 較淺深灰 */

/* 文字 */
--text-primary:   #e8eaed   /* 亮灰 */
--text-secondary: #9ca3af   /* 中灰 */
--text-muted:     #6b7280   /* 深灰 */

/* 重點色 */
--accent-blue:    #3b82f6   /* 主藍 */
--accent-green:   #10b981   /* 成功綠 */
--accent-orange:  #f59e0b   /* 警告橙 */
--accent-red:     #ef4444   /* 危險紅 */

/* 邊框和分隔 */
--border:         #374151   /* 邊框灰 */
--divider:        #1f2937   /* 分隔線 */
```

### 淺色備選

```css
--bg-primary:     #ffffff
--bg-secondary:   #f9fafb
--text-primary:   #111827
--accent-blue:    #2563eb
```

---

## 🧩 核心 React 元件架構

### 元件樹

```
App.jsx
├── Header
│   ├── Title
│   ├── SettingsButton
│   └── HelpButton
├── MainLayout (3-column)
│   ├── LeftPanel
│   │   ├── SpectrumUploader
│   │   ├── PeakDetectionControl
│   │   ├── RuleMatchingControl
│   │   └── ReviewControl
│   ├── CenterPanel
│   │   ├── SpectrumChart
│   │   └── PeakInfo
│   └── RightPanel
│       ├── PeakDetails
│       ├── CandidatesList
│       └── PeaksTable (toggle)
└── StatusBar
    ├── Progress
    ├── Stats
    └── Messages
```

---

## 💻 具體元件實現指南

### 1. App.jsx 架構

```javascript
function App() {
  const spectrum = useAppStore(state => state.spectrum)
  const peaks = useAppStore(state => state.peaks)
  const ui = useAppStore(state => state.ui)
  
  return (
    <div className="app-container">
      <Header />
      <MainLayout>
        <LeftPanel />
        <CenterPanel />
        <RightPanel />
      </MainLayout>
      <StatusBar />
    </div>
  )
}
```

### 2. MainLayout 佈局

```css
.main-layout {
  display: grid;
  grid-template-columns: 250px 1fr 360px;
  gap: 1px;
  height: calc(100vh - 120px);
  background: var(--border);
}

.left-panel, .center-panel, .right-panel {
  background: var(--bg-secondary);
  overflow-y: auto;
  padding: 16px;
}

/* 響應式 */
@media (max-width: 1400px) {
  .main-layout {
    grid-template-columns: 1fr;
    grid-template-rows: auto auto auto;
  }
}
```

### 3. 典型元件樣式 (Tailwind 或原始 CSS)

```css
.control-group {
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 12px;
  margin-bottom: 16px;
  background: var(--bg-tertiary);
}

.control-group h3 {
  font-size: 14px;
  font-weight: 600;
  margin-bottom: 8px;
  color: var(--text-primary);
}

.slider {
  width: 100%;
  height: 4px;
  background: var(--border);
  border-radius: 2px;
  outline: none;
}

.slider::-webkit-slider-thumb {
  appearance: none;
  width: 14px;
  height: 14px;
  border-radius: 50%;
  background: var(--accent-blue);
  cursor: pointer;
}

.button {
  width: 100%;
  padding: 8px 12px;
  background: var(--accent-blue);
  color: white;
  border: none;
  border-radius: 6px;
  font-size: 14px;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.2s;
}

.button:hover {
  background: #2563eb;
  opacity: 0.9;
}

.button:disabled {
  background: var(--text-muted);
  cursor: not-allowed;
}
```

---

## 🎬 互動流程和動畫

### 典型使用流程

```
1. 上傳 CSV
   [拖拽區放大] → 檔案驗證 → 光譜顯示 ✓

2. 檢測峰
   [按鈕 Loading...] → 計算進度 → 峰標記出現 ✓

3. 匹配規則
   [按鈕 Loading...] → 計算進度 → 結果填充 ✓

4. 審查結果
   點擊峰 → 右側展開詳情 → 對比候選 ✓

5. 導出
   選擇格式 → [下載] → 檔案保存 ✓
```

### 轉場動畫 (簡單)

```css
/* Fade in 圖表 */
.spectrum-chart {
  animation: fadeIn 0.3s ease-in;
}

/* Slide in 詳情面板 */
.peak-details {
  animation: slideInRight 0.2s ease-out;
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes slideInRight {
  from { transform: translateX(20px); opacity: 0; }
  to { transform: translateX(0); opacity: 1; }
}
```

---

## 📱 響應式設計

### Breakpoints

```
Desktop:  > 1400px  → 3 欄 (最優)
Tablet:   768-1400 → 2 欄 或垂直堆疊
Mobile:   < 768px  → 1 欄 垂直堆疊
```

### Mobile 視圖 (< 768px)

```
┌──────────────────────┐
│  FTIR Annotator      │
├──────────────────────┤
│  UPLOAD              │
│  ┌────────────────┐  │
│  │ 選擇 CSV 文件  │  │
│  └────────────────┘  │
├──────────────────────┤
│  SPECTRUM (collapsed)│
│  [📈 Expand]       │
├──────────────────────┤
│  RESULTS             │
│  [Peak 1]           │
│  [Peak 2]           │
│  ...                 │
├──────────────────────┤
│ Status: Ready       │
└──────────────────────┘
```

---

## 🎨 詳細的視覺反饋

### 狀態指示

```javascript
// 不同狀態的視覺反饋
const statusStyles = {
  'idle':     { bg: '#6b7280', icon: '⏸️' },
  'loading':  { bg: '#f59e0b', icon: '⚙️' },
  'success':  { bg: '#10b981', icon: '✓' },
  'error':    { bg: '#ef4444', icon: '✕' }
}
```

### 信心度可視化

```
信心度 0.92
████████░░ 92%  ← 進度條

信心度低 (< 0.5)
██░░░░░░░░ 45%  ⚠️ 需要審查

信心度歧義 (gap < 5%)
███████░░░ 87%  vs  87% 相似
                ⚠️ 多個候選接近
```

---

## 🔄 用戶交互示例

### 上傳光譜

```
1. 點擊或拖拽 CSV → 檔案預覽
2. 驗證 ✓ → 顯示數據點和波數範圍
3. 自動載入 → 光譜圖出現
4. Ready → "Detect Peaks" 按鈕激活
```

### 選擇峰

```
1. 在圖表上 Hover → 工具提示顯示 (波數, %T)
2. 點擊峰 → 
   - 峰被高亮
   - 右側詳情面板展開
   - 顯示 Top 5 候選
3. 點擊候選 → 分解信心度計分過程
```

### 歧義審查

```
1. 見到 ⚠️ Ambiguous 標記 → 相鄰 2-3 個候選很接近
2. 檢查相鄰峰 → 
   - 有 C-O stretch? → 偏向 Ester
   - 無? → 偏向 Ketone
3. 手動確認 → 選擇最可能的候選
```

---

## 📊 資訊呈現優先序

### 最重要（最突出）

```
1. 光譜圖表 (中央 50%)
   - 用戶每秒看無數次
   - 最大空間
   
2. 選中峰的詳情 (右側)
   - 當前分析的焦點
   - 信心度和候選明顯
   
3. 控制面板 (左側)
   - 執行動作的地方
   - 清晰的步驟順序
```

### 次要（次突出）

```
4. 全峰表 (可折疊)
5. 統計信息 (狀態欄)
6. 幫助和設置 (菜單)
```

---

## 🎯 設計決策理由

### 為什麼選擇 3 欄?

✓ 最大化光譜可視化空間
✓ 同時看到控制、圖表、結果
✓ 專家級工作流程效率高

### 為什麼是深色主題?

✓ 減少眼睛疲勞 (長時間使用)
✓ 科學軟件標準外觀
✓ 圖表顯示效果好

### 為什麼 Plotly?

✓ 互動性強 (zoom, pan, hover)
✓ 專業外觀
✓ 輕量級 (相對)

---

## 💡 可選增強

### Phase 2 可以加

- [ ] 暗/亮模式切換
- [ ] 快捷鍵 (Ctrl+D 檢測, Ctrl+M 匹配)
- [ ] 撤銷/重做
- [ ] 標註範本
- [ ] 批量標註模式
- [ ] 導入/導出標註

---

## 📐 尺寸參考 (px)

```
間距: 8, 12, 16, 24, 32
邊框: 1-2 px
圓角: 4-8 px
陰影: 0 1px 3px rgba(0,0,0,0.3)
字體大小: 12, 14, 16, 18, 20
行高: 1.5, 1.6
```

---

## 🚀 實現順序

### Week 1: 佈局 + 基本樣式
```
1. Header 和 StatusBar
2. 3 欄主佈局
3. 基本樣式和色彩
```

### Week 2: 左欄和中欄
```
1. SpectrumUploader
2. SpectrumChart (Plotly)
3. 控制面板基本框架
```

### Week 3: 右欄和表格
```
1. PeakDetails 面板
2. CandidatesList
3. PeaksTable
```

### Week 4: 打磨
```
1. 互動細節
2. 響應式優化
3. 動畫和轉場
4. 可訪問性 (a11y)
```

---

## 📚 CSS 框架選擇

### 推薦：Tailwind CSS

```
npm install -D tailwindcss postcss autoprefixer
```

好處：
- 快速開發
- 一致性高
- 深色模式內置

或者：**原始 CSS + CSS 變數** (更輕量)

```css
:root {
  --bg-primary: #0f1419;
  --text-primary: #e8eaed;
  /* ... 其他變數 */
}
```

---

## ✨ 最終設計總結

| 面向 | 方案 |
|------|------|
| **佈局** | 3 欄 (控制 + 圖表 + 結果) |
| **主題** | 深色專業 |
| **圖表** | Plotly.js |
| **互動** | 點擊峰 → 展開詳情 |
| **響應** | Grid + Media queries |
| **動畫** | 簡單淡入/滑出 |
| **字體** | 系統字體 (快速載入) |

---

**下一步**: 開始開發 React 元件！

需要我幫你寫：
1. SpectrumUploader 完整代碼？
2. App.jsx 基礎框架？
3. CSS 樣式表？

告訴我！
